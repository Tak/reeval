source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in reeval.gemspec
gemspec
gem 'shortbus', github: 'Tak/shortbus', branch: 'master'
gem 'fixedqueue', github: 'Tak/fixedqueue', branch: 'master'
